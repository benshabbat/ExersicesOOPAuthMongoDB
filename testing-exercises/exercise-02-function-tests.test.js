// תרגיל 02 - בדיקות פונקציות
// =============================
// הוראות: צור פונקציות וכתוב בדיקות עבורן
// הרץ עם: node --test exercise-02-function-tests.test.js

import { test } from 'node:test';
import assert from 'node:assert';

// ===========================================
// תרגיל 2.1 - פונקציית חיבור
// ===========================================

function add(a, b) {
  // כתוב את הפונקציה כאן
}

test('add: 2 + 3 = 5', () => {
  // כתוב בדיקה
});

test('add: 10 + 20 = 30', () => {
  // כתוב בדיקה
});

test('add: -5 + 5 = 0', () => {
  // כתוב בדיקה
});

// ===========================================
// תרגיל 2.2 - פונקציית כפל
// ===========================================

function multiply(a, b) {
  // כתוב את הפונקציה כאן
}

test('multiply: 2 * 3 = 6', () => {
  // כתוב בדיקה
});

test('multiply: 5 * 0 = 0', () => {
  // כתוב בדיקה
});

test('multiply: -2 * 3 = -6', () => {
  // כתוב בדיקה
});

// ===========================================
// תרגיל 2.3 - פונקציית ברכה
// ===========================================

function greet(name) {
  // כתוב את הפונקציה כאן
  // צריך להחזיר "Hello, [name]!"
}

test('greet: שלום לאליס', () => {
  // כתוב בדיקה
});

test('greet: שלום לבוב', () => {
  // כתוב בדיקה
});

// ===========================================
// תרגיל 2.4 - בדיקת זוגי/אי-זוגי
// ===========================================

function isEven(num) {
  // כתוב את הפונקציה כאן
  // החזר true אם זוגי, false אם אי-זוגי
}

test('isEven: 4 הוא זוגי', () => {
  // כתוב בדיקה
});

test('isEven: 7 הוא אי-זוגי', () => {
  // כתוב בדיקה
});

test('isEven: 0 הוא זוגי', () => {
  // כתוב בדיקה
});

// ===========================================
// תרגיל 2.5 - אורך מחרוזת
// ===========================================

function getStringLength(str) {
  // כתוב את הפונקציה כאן
}

test('getStringLength: אורך של "hello"', () => {
  // כתוב בדיקה
});

test('getStringLength: אורך של ""', () => {
  // כתוב בדיקה
});

// ===========================================
// תרגיל 2.6 - מקסימום
// ===========================================

function max(a, b) {
  // כתוב את הפונקציה כאן
}

test('max: המקסימום בין 5 ו-10', () => {
  // כתוב בדיקה
});

test('max: המקסימום בין 100 ו-50', () => {
  // כתוב בדיקה
});

test('max: המקסימום בין 7 ו-7', () => {
  // כתוב בדיקה
});

// ===========================================
// תרגיל 2.7 - טווח מספרים
// ===========================================

function range(start, end) {
  // כתוב את הפונקציה כאן
  // החזר מערך של מספרים מ-start עד end
}

test('range: 1 עד 3', () => {
  // כתוב בדיקה
  // רמז: השתמש ב-assert.deepStrictEqual
});

test('range: 5 עד 8', () => {
  // כתוב בדיקה
});

// ===========================================
// תרגיל 2.8 - המרה לאותיות גדולות
// ===========================================

function toUpperCase(str) {
  // כתוב את הפונקציה כאן
}

test('toUpperCase: המרת "hello"', () => {
  // כתוב בדיקה
});

test('toUpperCase: המרת "world"', () => {
  // כתוב בדיקה
});

// ===========================================
// תרגיל 2.9 - ריבוע
// ===========================================

function square(num) {
  // כתוב את הפונקציה כאן
}

test('square: ריבוע של 2', () => {
  // כתוב בדיקה
});

test('square: ריבוע של 5', () => {
  // כתוב בדיקה
});

test('square: ריבוע של 0', () => {
  // כתוב בדיקה
});

// ===========================================
// תרגיל 2.10 - מחשבון
// ===========================================

function calculate(operation, a, b) {
  // כתוב את הפונקציה כאן
  // תמיכה ב: "add", "subtract", "multiply", "divide"
}

test('calculate: חיבור', () => {
  // כתוב בדיקה
});

test('calculate: חיסור', () => {
  // כתוב בדיקה
});

test('calculate: כפל', () => {
  // כתוב בדיקה
});

test('calculate: חילוק', () => {
  // כתוב בדיקה
});

console.log('\n✅ סיימת את תרגיל 02!');
console.log('💡 עבור לתרגיל הבא: exercise-03-assertions.test.js');
