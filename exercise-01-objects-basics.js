/**
 * תרגיל 1: יצירת אובייקטים בסיסיים
 * רמת קושי: קלה
 * 
 * מטרה: להבין את המושגים הבסיסיים של אובייקטים ב-JavaScript
 */

// חלק א': צור אובייקט שמייצג סטודנט
// האובייקט צריך להכיל:
// - שם (name)
// - גיל (age)
// - ממוצע ציונים (averageGrade)

// כתוב את הקוד שלך כאן:
const student = {
    // הוסף את המאפיינים כאן
};

// חלק ב': הדפס את כל המאפיינים של הסטודנט
console.log("שם הסטודנט:", student.name);
// הוסף console.log לשאר המאפיינים


// חלק ג': הוסף מאפיין חדש לאובייקט
// הוסף מאפיין "major" (מגמה/תואר) לאובייקט student
// כתוב את הקוד שלך כאן:


// חלק ד': שנה את ערך הממוצע
// עדכן את ערך averageGrade ל-95
// כתוב את הקוד שלך כאן:


// ============ בדיקות ============
console.log("\n=== בדיקת תרגיל 1 ===");
console.log("האובייקט המלא:", student);

/**
 * פלט צפוי:
 * שם הסטודנט: [השם שבחרת]
 * גיל: [הגיל שבחרת]
 * ממוצע: [הממוצע שבחרת]
 * האובייקט המלא: { name: '...', age: ..., averageGrade: 95, major: '...' }
 */
